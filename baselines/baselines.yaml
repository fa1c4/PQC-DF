baselines:
  cryptofuzz:
    path: baselines/cryptofuzz
    dockerfile: baselines/cryptofuzz/Dockerfile
    build_dir: workspace/cryptofuzz/targets-build
    run_dir: workspace/cryptofuzz/targets-run
    dispatcher_name: cryptofuzz
    notes: "Vendored baseline fuzzer. Upstream Git metadata removed."

  CLFuzz:
    path: baselines/CLFuzz
    dockerfile: baselines/CLFuzz/Dockerfile
    build_dir: workspace/CLFuzz/targets-build
    run_dir: workspace/CLFuzz/targets-run
    dispatcher_name: CLFuzz
    notes: "Vendored baseline fuzzer. Upstream Git metadata removed."

  libFuzzer:
    path: baselines/libFuzzer
    dockerfile: baselines/libFuzzer/Dockerfile
    build_dir: workspace/libFuzzer/targets-build
    run_dir: workspace/libFuzzer/targets-run
    dispatcher_name: libFuzzer
    notes: "Standalone stock libFuzzer baseline for liboqs KEM/SIG lifecycle fuzzing."

  cryptoTesting:
    path: baselines/cryptoTesting
    dockerfile: baselines/cryptoTesting/Dockerfile
    build_dir: workspace/cryptoTesting/targets-build
    run_dir: workspace/cryptoTesting/targets-run
    dispatcher_name: cryptoTesting
    notes: "Vendored baseline fuzzer. Upstream Git metadata removed."
